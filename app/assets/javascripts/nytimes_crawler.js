// Runs in console
/*
var currentArr = [];
$$('#main .story').each(function(i, item){
  var $this   = $$($$(this));
  var date    = Date();
  var title   = $this.find('h3').innerText;
  var content = $this.find('.summary').innerText;
  var currentObj = {url: url, title: title, content: content}
  currentArr = currentArr.push(currentObj);
});
myRootRef.push(currentArr);
*/
